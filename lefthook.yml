pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "tools/src/**/*.ts"
      run: npx biome check --fix --staged {staged_files}
      stage_fixed: true

pre-push:
  parallel: false
  commands:
    typecheck:
      run: npm run typecheck
    test:
      run: npm test
